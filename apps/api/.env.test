DATABASE_URL="postgresql://raisely:raisely@localhost:5432/raisely_test?schema=public"
REDIS_URL="redis://localhost:6379/1"

# Fixed, checked-in secret for signing test JWTs (see src/test/auth.ts) - not
# a real secret, just needs to be consistent across test runs and CI.
AUTH_SECRET="raisely-integration-test-secret-do-not-use-in-prod"
